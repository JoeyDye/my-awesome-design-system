import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'
import { action } from '@storybook/addon-actions'
import { withKnobs, boolean } from '@storybook/addon-knobs'
import { SignUpModal } from './Modals'

<Meta title='Design System|Modals' component={SignUpModal} />

# Modals

Modals are dialogs which overlay content to convey a message.

## Usage

Use modals to call a user to action or complete a task.

## Sign Up Modal

The sign up modal is used to prompt a user to create an account

<Preview withToolbar>
  <Story name='Sign Up Modal'>
    <SignUpModal showModal={boolean('Show modal', true)} />
  </Story>
</Preview>
